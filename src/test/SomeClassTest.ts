import * as assert from 'assert';

import { SomeClass } from '../app/SomeClass';

describe('Some class', () => {

    let server;


    it("can be constructed", () => {
        const someClass = new SomeClass();
    });

});
